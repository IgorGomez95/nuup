// import { useFetchActions } from "../../hooks/useFetchActions";

export const plaguicidasReducer = (state = [], action) => {
    switch (action.type) {
        case 'add':
            // const { loading, data } = useFetchActions('http://localhost/nuup/backend/public/api/v1/plaguicidas','POST',action.payload);
            // return data;
            
        default:
            break;
    }
}
